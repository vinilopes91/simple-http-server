1 - Compilar o projeto com um dos comandos `make`, `make install` ou `gcc -g -pthread -O0 -Wall server.c dqueue.c -o http-server` para gerar o executavel 'http-server'
2 - Execute o programa gerado como root
3 - Para fazer o download de arquivos rodando na mesma pasta que o servidor envie requisições GET para localhost

Ex de request url: http://localhost/LEIAME.txt
